
<h1>記録</h1>


<h3><%= link_to '日記をかく', new_blog_path %></h3>

<%= month_calendar events: @user.blogs do |date, blogs| %>
  <%= date.day %>

  <% blogs.each do |blog| %>
    <div>
      <%= link_to blog.title, blog %>
    </div>
  <% end %>
<% end %>