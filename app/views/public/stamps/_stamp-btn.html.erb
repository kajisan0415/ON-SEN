  <% if article.stamped_by?(current_user) %>
  <p><%=link_to "★", article_stamps_path(article.id), method: :delete, remote: true %></p>

  <% else %>
  <p><%=link_to "☆", article_stamps_path(article.id), method: :post, remote: true %></p>
  <% end %>